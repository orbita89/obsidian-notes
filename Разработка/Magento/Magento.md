Подключение шаблона $this->setTemplate('catalog/product/tab/video.phtml');
Бывает магические методы которые не понятно от куда беруться иногда это может быть аттрибуты а вот иногда это может быть форма в html коде например:  вызываеться метод $product->getGenderVideo()  неизвестно откуда он взялся потом он преобразуеться в  call()  а там уже getData('gender_video') но изначально мы это берем из  ``<input type="hidden" name="product[gender_video][<?= $genderId ?>][thumbnail]"  value="<?= $video->getThumbnail() ?>"> это пример в задаче на мадженте [[gender_video]]
Мадженто делает сначала один запрос в нём он сабирает почти все данные(аттрибуты) и кэширует его 