1. Подключение шаблона $this->setTemplate('catalog/product/tab/video.phtml');
2. Бывает магические методы которые не понятно от куда беруться иногда это может быть аттрибуты а вот иногда это может быть форма в html коде например:  вызываеться метод $product->getGenderVideo()  неизвестно откуда он взялся потом он преобразуеться в  call()  а там уже getData('gender_video') но изначально мы это берем из  ``<input type="hidden" name="product[gender_video][<?= $genderId ?>][thumbnail]"  value="<?= $video->getThumbnail() ?>"> это пример в задаче на мадженте [[gender_video]]
3. Мадженто делает сначала один запрос в нём он сабирает почти все данные(аттрибуты) и кэширует его
4. Транкзации фактически запускаються вот так. Пока транзакция не зафиксирована. другие соединения не видят изменений при ошибке можно вернуть в изходное положения БД.
```
START TRANSACTION;
-- твои SQL-запросы
COMMIT;
-- или
ROLLBACK;
```
5. Импорт в magento:  1) Импорт продуктов нужно проверить список аттрибутов в самом начале надо быть увереным что все обязательные поля есть. Еще очень важно не много не каректное названиие у Fast Import Attribute она обновляет текущие аттрибуты у продукта тоесть продукт уже должен быть в таблице, поэтому если мы ходим добавить новый продукт то нужно убрать эту галочку. Сам процесс сначала мы добавляем в таблицу dataflow_batch_import потом так же добавляем записи в таблицу dataflow_batch все значения которые мы импортируем. следущий этап сохранения и идем дальше после того как данные сохранились мы их вынимаем из таблиц dataflow_batch и уже после парсинга csv и после проверки обязадельных полей добавляем и сохраняем в таблицу product_entity. 2) Импорт сортировок 