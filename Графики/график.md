```dataviewjs
// ====== НАСТРОЙ И ЭНЕРГИЯ — ПЕРЕКЛЮЧАЕМЫЙ ГРАФИК ======
const MODE = "week"; // "week" или "month"

const notes = dv.pages('"Ежедневник"')
    .where(p => p.дата && p.настрой != null && p.энергия != null)
    .sort(p => p.дата, 'asc');

if (!notes.length) {
    dv.paragraph("Нет данных для графика");
} else {

    const groups = new Map();

    for (let n of notes) {
        const dt = dv.luxon.DateTime.fromISO(n.дата);
        const key = MODE === "week"
            ? `${dt.weekYear}-W${String(dt.weekNumber).padStart(2,'0')}`
            : dt.toFormat("yyyy-MM");

        if (!groups.has(key)) groups.set(key, []);
        groups.get(key).push({
            mood: Number(n.настрой),
            energy: Number(n.энергия)
        });
    }

    const labels = [];
    const mood = [];
    const energy = [];

    for (let [k, arr] of groups.entries()) {
        labels.push(k);
        mood.push(arr.reduce((a,e)=>a+e.mood,0)/arr.length);
        energy.push(arr.reduce((a,e)=>a+e.energy,0)/arr.length);
    }

    dv.el("div", `
\`\`\`chart
type: line
labels: ["${labels.join('","')}"]
series:
  - name: Настрой
    data: [${mood.map(v=>v.toFixed(2)).join(',')}]
  - name: Энергия
    data: [${energy.map(v=>v.toFixed(2)).join(',')}]
options:
  title: "Динамика состояния (${MODE === "week" ? "по неделям" : "по месяцам"})"
  legend: true
  yAxis:
    min: 0
    max: 10
\`\`\`
`, { cls: "markdown" });

}


```

```dataviewjs


```
```dataviewjs
// ====== СРАВНЕНИЕ ПО МЕСЯЦАМ ======
const notes = dv.pages('"Ежедневник"')
    .where(p => p.дата && p.настрой != null)
    .sort(p => p.дата, 'asc');

const months = new Map();

notes.forEach(n => {
    const m = dv.luxon.DateTime.fromISO(n.дата).toFormat("yyyy-MM");
    if (!months.has(m)) months.set(m, []);
    months.get(m).push(Number(n.настрой));
});

const stats = [];
for (let [m, arr] of months.entries()) {
    stats.push({
        month: m,
        avg: arr.reduce((a,b)=>a+b,0)/arr.length
    });
}

stats.sort((a,b)=>b.avg-a.avg);

dv.table(["Месяц","Средний настрой"], stats.map(s=>[s.month, s.avg.toFixed(2)]));

```
### 🔹 Что делает этот ГРАФИК:

1. Берёт все записи из `Ежедневник`.
    
2. Вычисляет **номер недели в году** для каждой даты.
    
3. Группирует `настрой` и `энергию` по неделям и берёт **среднее значение** за неделю.
    
4. Строит график, где по оси X — недели, по Y — баллы от 0 до 10.

```dataviewjs
// ====== АВТОАНАЛИЗ ТЕКУЩЕГО МЕСЯЦА ======

const now = dv.luxon.DateTime.now().toFormat("yyyy-MM");

const notes = dv.pages('"Ежедневник"')
    .where(p => p.дата && p.настрой != null && p.энергия != null &&
        dv.luxon.DateTime.fromISO(p.дата).toFormat("yyyy-MM") === now)
    .array(); // ← ВАЖНО

if (notes.length === 0) {

    dv.paragraph("### 🧾 Итог месяца");
    dv.paragraph("В этом месяце пока нет записей с данными о настроении и энергии.");

} else {

    const avgMood = notes.reduce((a,n)=>a+Number(n.настрой),0)/notes.length;
    const avgEnergy = notes.reduce((a,n)=>a+Number(n.энергия),0)/notes.length;

    let moodText = avgMood >= 7 ? "месяц был эмоционально хорошим"
                 : avgMood >= 5 ? "месяц был умеренным"
                 : "месяц выдался тяжёлым";

    let energyText = avgEnergy >= 7 ? "уровень энергии был высоким"
                   : avgEnergy >= 5 ? "энергия была нестабильной"
                   : "ощущалась усталость и снижение сил";

    dv.paragraph("### 🧾 Итог месяца");
    dv.paragraph(
        `В среднем настрой: **${avgMood.toFixed(1)}**, энергия: **${avgEnergy.toFixed(1)}**.  
Можно сказать, что **${moodText}**, при этом **${energyText}**.`
    );
}
```



